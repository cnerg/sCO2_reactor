version: 2

jobs:
    test:
        docker:
            - image: pyne/pyne_ubuntu_16.04
        steps:
            - checkout
            - run:
                 name: run tests
                 command: |
                     apt-get update
                     apt-get install -y python3-pip
                     pip3 install pytest
                     pytest
workflows:  
    version: 2
    test:
        jobs:
            - test
