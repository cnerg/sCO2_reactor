version: 2

jobs:
    test:
        docker:
            - image: circleci/python:3.6.1
        steps:
            - checkout
            - run: pip install pytest
            - run:
                 name: run tests
                 command: |
                     cd ./optimization
                     pytest
workflows:
    version: 2
    test:
        jobs:
            - test
